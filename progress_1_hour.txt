The change made since the last commit is in the `studio_chat_app.py` file. Specifically, the `send_message` function has been updated. The URL construction in this function now points to `/message/{assistant_id}` instead of `/studio/{assistant_id}`. This change is evident in the line `url = f"{st.session_state.base_url.rstrip('/')}/message/{assistant_id}"`, which replaces the original line `url = f"{st.session_state.base_url.rstrip('/')}/studio/{assistant_id}"`. Additionally, the `progress_1_hour.txt` file has been deleted as it previously contained the text "There are no changes to describe."