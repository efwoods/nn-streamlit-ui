The changes made since the last commit are clear. The `progress_1_hour.txt` file has been deleted, and previously, it contained a description of changes made to the `studio_chat_app.py` file. According to the deleted text, the `send_message` function in `studio_chat_app.py` was updated to change the URL construction from `/studio/{assistant_id}` to `/message/{assistant_id}`. Specifically, the line `url = f"{st.session_state.base_url.rstrip('/')}/studio/{assistant_id}"` was replaced with `url = f"{st.session_state.base_url.rstrip('/')}/message/{assistant_id}"`. That's the update that was made.